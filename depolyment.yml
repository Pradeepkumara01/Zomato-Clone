apiVersion: apps/v1
kind: Deployment
metadata:
  name: zomato-app
  labels:
    app: zomato-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: zomato-app
  template:
    metadata:
      labels:
        app: zomato-app
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: zomato-app
        image: pradeepkumara01/zomato:latest
        imagePullPolicy: "Always"
        ports:
        - containerPort: 3000

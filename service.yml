apiVersion: v1
kind: Service
metadata:
  name: zomato-clone-app
  labels:
    app: zomato-clone-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: zomato-clone-app
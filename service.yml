apiVersion: v1
kind: Service
metadata:
  name: zomato-app
  labels:
    app: zomato-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: zomato-app
